name: diagnostico CI

on:
  pull_request:
    branches: [develop, main]

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      diagnostico: ${{ steps.filter.outputs.diagnostico }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            diagnostico:
              - 'apps/diagnostico/**'
              - '.github/workflows/diagnostico-ci.yml'

  diagnostico:
    needs: changes
    if: needs.changes.outputs.diagnostico == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/diagnostico
    env:
      NEXT_PUBLIC_API_URL: http://localhost:3000
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.4.0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: apps/diagnostico/pnpm-lock.yaml

      - run: pnpm install --frozen-lockfile
      - run: pnpm audit --audit-level=high
      - run: pnpm lint
      - run: pnpm build
