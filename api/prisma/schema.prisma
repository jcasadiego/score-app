// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

/// Bitácora de auditoría: registra toda acción de escritura hecha a través
/// de la API (requisito duro de P4). El detalle de qué se cambió va en
/// `datos`, ya que la forma exacta depende de cada entidad.
model RegistroAuditoria {
  id         String   @id @default(cuid())
  accion     String
  entidad    String
  entidadId  String?
  usuarioId  String?
  datos      Json?
  creadoEn   DateTime @default(now())

  @@index([entidad, entidadId])
  @@index([creadoEn])
}

/// Versión de reglas aplicada a un diagnóstico (requisito duro de P4).
/// El contenido de las reglas es configuración, no código — vive en
/// `contenido` mientras se define la especificación completa del
/// cuestionario y su lógica.
model VersionReglas {
  id         String   @id @default(cuid())
  numero     Int      @unique @default(autoincrement())
  contenido  Json
  activa     Boolean  @default(false)
  creadoEn   DateTime @default(now())
}

enum RolUsuario {
  ADMIN
  // TODO: el modelo real de roles/permisos de apps/panel todavía no está
  // definido (ver CLAUDE.md sección 3, pendiente "modelo de login/
  // permisos de apps/panel") — confirmar con José antes de agregar más
  // valores o de asumir que este enum es la forma final.
}

/// Placeholder de autenticación para apps/panel. TODO: el modelo real de
/// permisos (roles múltiples, alcance por caso, etc.) es un pendiente
/// explícito del proyecto — este modelo solo existe para que el
/// mecanismo de login JWT tenga contra qué autenticar mientras tanto.
model Usuario {
  id            String     @id @default(cuid())
  email         String     @unique
  passwordHash  String
  rol           RolUsuario @default(ADMIN)
  creadoEn      DateTime   @default(now())
  actualizadoEn DateTime   @updatedAt
}
